# .gitignoreはAIに書かせたのでミスがあるかもしれないです。

# --- General / OS ---
.DS_Store
Thumbs.db
*.log

# --- VS Code ---
.vscode/
*.code-workspace
# プロジェクト固有の設定を共有したい場合は .vscode/settings.json などは除外しない運用もありますが、
# 個人開発ならまるごと無視でOKです。

# --- Python (Master Server) ---
# Byte-compiled / Optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual Environments (仮想環境)
# ※環境名が venv, env, .venv のどれでも対応できるようにしています
venv/
env/
.venv/

# Environment Variables (APIキーなどは絶対にコミットしない！)
.env
.env.local

# Flask specific
instance/
.webassets-cache

# --- Flutter / Dart (Worker App) ---
# Libraries and tools
.dart_tool/
.packages
.pub/
.pub-cache/

# Build artifacts (ビルド生成物)
build/
worker_app/build/

# Android specific (Flutter)
# 自分のPCのSDKパスなどが書かれるので絶対に無視する
**/android/local.properties
**/android/key.properties

# Android Studio / Gradle files
.gradle/
.idea/
**/android/.gradle
**/android/app/build
**/android/captures
**/android/gradlew
**/android/gradlew.bat
# gradlew は本来コミットすべきですが、Flutterプロジェクトでは自動生成されるため
# 環境差異を避けるために無視する場合もあります。
# ただし、チーム開発なら gradlew はコミットしたほうが安全です。
# 今回は一人開発なので、トラブル回避で無視リストには入れず、local.propertiesだけ徹底的に無視します。

# iOS specific (Flutter)
**/ios/Flutter/Generated.xcconfig
**/ios/Flutter/flutter_export_environment.sh
**/ios/.generated/

# --- Whisper / Large Files ---
# 音声データやモデルファイルなどの巨大ファイル
*.mp3
*.wav
*.m4a
uploads/
models/
*.bin
*.tflite

testing_area/